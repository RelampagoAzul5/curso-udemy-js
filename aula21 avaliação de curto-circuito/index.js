/*
&& -> false && true -> false "o valor mesmo"
function falaOi(){
    return 'Oi';
};

let vaiExecutar = 'fasfas';

console.log(vaiExecutar && falaOi());


|| -> true || false -> vai retornar "o valor verdadeiro"
console.log(0 || false || null || 'Giovanni' || true); // -> retorna 'Giovanni'

const corUsuario = 'vermelho';
const corPadrao = corUsuario || 'preto'; // -> se a variavel corUsuario tiver um valor, retorna o valor de corUsuario, se não retorna o valor padrão que no caso é preto

console.log(corPadrao)

FALSY
false
0
'' "" ``
null / undefined
NaN
*/
